import { AuthDisabled } from "../../ui/atoms/auth-disabled";

const defaults = {
  title: "Atoms/Auth Disabled",
};

export default defaults;

export const AuthDisabledStory = () => <AuthDisabled />;
