<%
// Inserts a link to an XPCOM Interface reference page.
// Appropriate styling is applied.
//
// Parameters:
//
//  $0 - Interface name

// Deprecated in April 2022, as no longer used in en-US.
// Delete once no longer used in translated-content.
mdn.deprecated();

let lang = env.locale;
let basePath = "/" + lang + "/docs/Mozilla/Tech/XPCOM/Reference/Interface/";

switch(lang) {
  case 'de':
    basePath = "/de/docs/XPCOM_Interface_Referenz/";
    break;
}

const link = web.smartLink(basePath + $0, null, `<code>${$0}</code>`, $0, basePath);

// Note: Tooltips are disabled at present. We need to fix how this works with pages that use the IFSummary template
%><%- link %>
