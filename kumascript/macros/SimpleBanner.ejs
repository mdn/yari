<%/* Creates a banner with some text but no icon.

    Parameters:
        $0 - Text to display as the badge heading line
        $1 - Class name for custom style
        $2 - Additional text to display

*/%>
<div class="notecard <%= $1 %>">
    <h4><%- $0 %></h4>
    <p><%- $2 %></p>
</div>
