import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import Logo from "./logo.tsx";

<Meta title="Atoms|Logo" component={Logo} />

# Logo

This is the main brand logo used in the header of the website.

## Usage

The logo is added to a page using the `Logo` component.

<Preview withToolbar>
  <Story name="logo">
    <Logo />
  </Story>
</Preview>
